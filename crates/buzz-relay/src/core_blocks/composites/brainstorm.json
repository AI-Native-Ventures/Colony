{
  "schema": "ai-native-office.block-manifest/1",
  "handle": "brainstorm",
  "version": "1.0.0",
  "created_at": 1785369600,
  "name": "Brainstorm",
  "description": "A structured multi-select question with selectable ideas and custom input.",
  "origin": "core",
  "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "additionalProperties": false,
    "required": ["title", "prompt", "choices"],
    "properties": {
      "title": { "type": "string", "minLength": 1, "maxLength": 160 },
      "prompt": { "type": "string", "minLength": 1, "maxLength": 1000 },
      "choices": {
        "type": "array",
        "minItems": 1,
        "maxItems": 12,
        "items": {
          "type": "object",
          "additionalProperties": false,
          "required": ["id", "title", "description"],
          "properties": {
            "id": { "type": "string", "pattern": "^[a-z][a-z0-9-]{0,63}$" },
            "title": { "type": "string", "maxLength": 120 },
            "description": { "type": "string", "maxLength": 500 }
          }
        }
      }
    }
  },
  "tree": {
    "type": "stack",
    "gap": "medium",
    "children": [
      {
        "type": "section",
        "title": "{{title}}",
        "text": "{{prompt}}"
      },
      {
        "type": "card-list",
        "items_path": "/choices",
        "card": {
          "type": "card",
          "title": "{{title}}",
          "description": "{{description}}",
          "children": []
        }
      },
      {
        "type": "question",
        "prompt": "Choose every direction worth exploring.",
        "mode": "multi-select",
        "options": [
          { "id": "premium", "label": "Premium editorial" },
          { "id": "motion", "label": "Cinematic motion" },
          { "id": "conversion", "label": "Conversion-led" }
        ],
        "min_selections": 1,
        "max_selections": 3,
        "allow_custom": true,
        "require_custom_input": false,
        "submit_action": "brainstorm.submit"
      }
    ]
  },
  "actions": [
    {
      "id": "brainstorm.submit",
      "label": "Submit directions",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "additionalProperties": false,
        "required": ["selected"],
        "properties": {
          "selected": {
            "type": "array",
            "minItems": 1,
            "maxItems": 3,
            "uniqueItems": true,
            "items": {
              "type": "string",
              "enum": ["premium", "motion", "conversion"]
            }
          },
          "custom_input": { "type": "string", "maxLength": 2000 }
        }
      },
      "interaction": {
        "type": "signed",
        "action_id": "brainstorm.submit",
        "resolves_attention": false
      },
      "permissions": []
    }
  ],
  "permissions": [],
  "fallback_template": "## {{title}}\n\n{{prompt}}\nChoices: {{choices}}\nSelect one or more directions or reply with your own.",
  "supported_clients": ["desktop"],
  "primitive_versions": {
    "section": 1,
    "card-list": 1,
    "card": 1,
    "question": 1
  },
  "examples": [
    {
      "name": "Homepage direction",
      "data": {
        "title": "Choose the homepage direction",
        "prompt": "Which qualities should the first concept combine?",
        "choices": [
          {
            "id": "premium",
            "title": "Premium editorial",
            "description": "Confident typography, restraint, and strong art direction."
          },
          {
            "id": "motion",
            "title": "Cinematic motion",
            "description": "A carefully paced hero with meaningful scroll transitions."
          },
          {
            "id": "conversion",
            "title": "Conversion-led",
            "description": "Clear proof, trust, and a focused call to action."
          }
        ]
      }
    }
  ],
  "validation": { "state": "tested", "requires_attention": false }
}
