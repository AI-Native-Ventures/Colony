# Requested behavior and completion audit

This audit separates an implemented surface from a usable and proven workflow. It is based on the September 9 owner report, current source and the preceding package check.

| Request | Current evidence | Remaining acceptance |
| --- | --- | --- |
| Every new community repeats relevant onboarding | Existing-profile skip repaired; creation entry, scoped resume and completion passed GitHub UI regressions | Packaged creation proof; new Scout must inherit Power even when background creation started earlier |
| Three useful Power choices | Connection, purchase and allowance flows passed GitHub UI checks at `fa055b5983` | Complete CI and packaged end-to-end proof on the final checkpoint |
| Detect subscriptions and limits | Official account/model/usage inspection, scoped connection, explicit provider installation and vendor execution bridge implemented; packaged work-tool isolation passed | Authenticated provider execution and cancellation remain separate gates; unknown limits remain unknown |
| Buy Colony Credits in onboarding | Gateway inference previously live-proven; purchase, recovery, paid-but-unfunded and balance reconciliation passed GitHub UI regressions | No live purchase authorized or claimed |
| OpenRouter $10/1,000-per-day check | OAuth, allowance check, provider top-up and return/recheck passed GitHub UI regressions | Normal OAuth may expose insufficient lifetime-purchase evidence, shown as unknown; no live top-up claimed |
| Cookie import Select all and clear outcome | Packaged Select all/Clear selection proven; result views covered in CI | Actual login validity still requires opening imported sites |
| Human names and agent roles | Existing owner name repaired and Scout role visible live; new-community name carry passed GitHub UI regression | Packaged new-community proof |
| Multicolour branded onboarding, readable chat, community rail | Previous final package visually inspected | Preserve these accepted surfaces while completing behavior |
| Orchestrator delegates real work to workers | Scout conversation runs on Credits | First-job staffing/delegation gate remains incomplete; conversation is not that proof |
| Main desktop port to Electron and production updates | Self-contained beta packaged and opened; main promotion delegated | Signed production Electron distribution and updater path, not just a Tauri release |
| Ready-to-use social media and website teammates | Product direction approved | Not claimed implemented by onboarding or generic inline cards |

All new tests and builds run in GitHub. Live purchases are not implied by UI verification; payment success requires an authoritative receipt and balance reconciliation.

## Current verification boundary

At checkpoint `fa055b5983`, GitHub passed Rust Unit Tests, Rust Lint, Desktop Core, Desktop Tauri Flags and all six browser smoke shards. New-community creation/resume/name carry and all three Power paths ran through the actual UI with a mock native bridge. This does not establish hosted community creation, real provider authentication or a payment. The payment regression separately supplies an authoritative synthetic receipt and then a funded balance. Rendered screens preserve the approved gradients and ants, with companion captures showing the lower controls. Saved subscriptions cannot bypass their business connection through generic settings; legacy Credits stays Credits until an explicit connection update.

Beta workflow `34352174621`, at checkpoint `1d7ad501d8`, passed relocated startup, signup and browser persistence checks and all 23 native isolation tests. The actual packaged work-tool wrapper could read and write worker files but could not read a synthetic private subscription profile, including through a child process or symlink. Unsandboxed positive controls verified that the private file was otherwise readable. This proves the Seatbelt boundary, not authenticated subscription inference. The subscription bridge uses the unmodified provider CLI and native-wrapped work tools. Codex must prove that its installed protocol supports disabling native work environments before launching.

The subsequent Scout fix removes automatic runtime pins from newly created Welcome agents. A background creation started before Power saves must still resolve the latest defaults at launch. Existing agent settings are preserved; onboarding never rewrites manual pins. Async creation and native runtime/model/tool inheritance regressions cover that race and require fresh GitHub checks. Complete CI and a package built from the final source checkpoint remain the acceptance gate. No local CI, test suites, or builds were run.

The separate release owner is handling PR #665 at the agreed develop cutoff, including WebKit-to-Chromium community/draft migration and the production Electron updater. It is not proof that PR #666 is released.
