# Requested behavior and completion audit

This audit separates an implemented surface from a usable and proven workflow. It is based on the September 9 owner report, current source and the preceding package check.

| Request | Current evidence | Remaining acceptance |
| --- | --- | --- |
| Every new community repeats relevant onboarding | Existing-profile skip repaired; real creation entry, owner/relay-scoped resume and completion implemented | GitHub regression and packaged creation proof |
| Three useful Power choices | Business-scoped Power step now contains connection, purchase and allowance flows | Passing GitHub checks and packaged end-to-end proof |
| Detect subscriptions and limits | Official provider account/model/usage inspection, scoped native connection, explicit missing-provider installation and direct vendor execution bridge implemented | Prove restricted execution and cancellation against the packaged app; unknown limits remain unknown |
| Buy Colony Credits in onboarding | Gateway inference previously live-proven; Power purchase control, durable recovery and receipt verification implemented | GitHub payment regression; no live purchase authorized or claimed |
| OpenRouter $10/1,000-per-day check | Connected OAuth, allowance check, provider top-up and return/recheck implemented | GitHub regression; normal OAuth may expose insufficient lifetime-purchase evidence, shown as unknown |
| Cookie import Select all and clear outcome | Packaged Select all/Clear selection proven; result views covered in CI | Actual login validity still requires opening imported sites |
| Human names and agent roles | Existing owner name repaired and Scout role visible live | Public owner name now carried into a new community; GitHub regression and packaged proof still pending |
| Multicolour branded onboarding, readable chat, community rail | Previous final package visually inspected | Preserve these accepted surfaces while completing behavior |
| Orchestrator delegates real work to workers | Scout conversation runs on Credits | First-job staffing/delegation gate remains incomplete; conversation is not that proof |
| Main desktop port to Electron and production updates | Self-contained beta packaged and opened; main promotion delegated | Signed production Electron distribution and updater path, not just a Tauri release |
| Ready-to-use social media and website teammates | Product direction approved | Not claimed implemented by onboarding or generic inline cards |

All new tests and builds run in GitHub. Live purchases are not implied by UI verification; payment success requires an authoritative receipt and balance reconciliation.

## Current verification boundary

PR #666 remains a draft. At checkpoint `1d7ad501d8`, GitHub passed Rust Unit Tests, Rust Lint, Desktop Core, and five of six browser smoke shards. New-community creation/resume/name carry passed through the actual UI with a mock native bridge. The remaining Power test wrongly expected a paid receipt to create a positive mock ledger balance; the next checkpoint verifies paid-but-zero first, then explicitly supplies the funded balance and checks again. Rendered screens preserve the approved gradients and ants; lower-control captures are added because the internal scrolling canvas does not expand with a full-page screenshot. Saved subscriptions now cannot bypass their business connection through generic settings, and legacy Credits stays Credits until an explicit connection update. Those latest readiness regressions still await GitHub. The macOS subscription work-tool test uses a synthetic private provider profile; actual Seatbelt execution is still pending in the beta workflow. The subscription bridge uses the unmodified provider CLI and native-wrapped work tools. Codex must prove that its installed protocol supports disabling native work environments before launching. No local CI, test suites, or builds were run.

The separate release owner is handling PR #665 at the agreed develop cutoff, including WebKit-to-Chromium community/draft migration and the production Electron updater. It is not proof that PR #666 is released.
