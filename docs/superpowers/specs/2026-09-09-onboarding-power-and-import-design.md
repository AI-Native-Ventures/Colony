# Founder setup and beta recovery

The owner has approved three onboarding choices for powering agents, browser import improvements, and repair of nameless identities and rejected Scout launches. Preserve the approved gradient-and-ants composition, production message typography, communities rail, channels and adjacent threads.

## Acceptance contract

- Account includes a human name and publishes it as identity-scoped profile metadata. Older nameless accounts have a direct repair action. Never fabricate agent or owner identities.
- Business details lead to a third, explicit Power your agents step. It shows detected subscriptions, Colony Credits and OpenRouter free models. The owner chooses a default provider and model before completion; secrets remain in native configuration, never onboarding localStorage.
- Readiness agrees with native launch policy. Detection does not imply support: the current Electron sandbox only launches Colony Agent. Detected subscription runtimes that lack an isolated adapter must be labelled unavailable and must not pass readiness. Enabling isolated subscription execution is a separate proof gate, not removal of the sandbox guard.
- Credits must not require a personal API key. An unavailable account/gateway is shown as unavailable, not a zero balance or a ready connection. OpenRouter free model selection must not silently fall back to paid models. Its $10 purchase threshold increases the allowance rather than enabling all free access (official OpenRouter limits, checked 2026-09-09).
- Cookie import offers explicit bulk selection, a count, progress, and distinct copied/preserved/skipped/failed outcomes. A zero-copy result is not presented as a successful login. Preserve replacement consent and native batching limits.
- The current first-job error says business approval is missing, although source expects every community to have a relay-minted profile. Diagnose that deployment/data path before inventing a new approval or bypassing task authority.

## Proof gates

1. Source diagnosis and focused implementation with regression cases.
2. GitHub CI and rendered screenshots of the three steps, bulk importer outcomes, name persistence and runtime availability.
3. Packaged macOS workflow for the changed native/UI paths. Synthetic proof is named as such; it does not establish personal subscription access or hosted gateway deployment.

The user explicitly requests GitHub CI only: do not run local tests, builds or CI hooks. Existing authorization covers implementation and PRs without repeating routine permission requests. Do not import personal cookies, purchase credits, launch paid model requests, or loosen isolation for testing.
